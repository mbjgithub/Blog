这个文件夹的作用是模块化博客网站的index.ejs
整个流程如下：
 首先webpack将所有的业务代码打包成一个js文件，并用gulp插到layout.html文件中
 在收到请求后，先去数据库服务器（我自己用Node写了一个提供数据的服务器，很简单的）拿数据，然后先递归把所有的tpl代码判断整合起来，在用ejs编译，渲染，最终返回给客户端

前端用了一个框架Real

但是我在构建Node数据库服务器的时候，发现了一个问题，详见Blog项目的routers下的sqlServer.js中的注释